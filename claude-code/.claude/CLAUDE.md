# Claude Code Project Note

This project can use the `clickup-operator` skill for ClickUp routing and task-handling workflows.

Use `clickup-operator` when the user wants:

- ideas captured into ClickUp
- action items created or routed
- assignees or due dates updated
- today's ClickUp work read back quickly

Keep reusable procedure logic in the skill itself, not in this file.
